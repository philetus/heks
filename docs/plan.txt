modules:

gooey: basic window api

pad: heks file viewer / editor

canon: data repository
  > symbol? leaf? term?
    * word
    * quantity [order, magnitude, degree, unit] (+i, +/+, +/-, 0, -/-, -/+, -i, nan)
    * boolean
    * variable
    * method
    * object
  > branch? phrase?
    * phrase
    * list
    * conjunction
    * disjunction
  > verse (line)
  > stanza (pp)
  > session (period of input from one or more agents)

agent:
  > open / join / commit session
  > move / expand / contract focus
  > pan / zoom [/ rotate / advance] gaze
  
  
